.admin-user-form
  %h3 Invite User
  = simple_form_for(user, url: admin_invitations_path, method: :post,  html: { class: 'form-horizontal' }) do |f|
    = f.input :email
    = f.input :first_name
    = f.input :last_name
    = f.input :occupation
    = f.input :country
    = f.association :roles,  as: :check_boxes
    = f.button :submit, 'Send Invitation', class: 'btn btn-success'
