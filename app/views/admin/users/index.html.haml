%h1
.create-user-links
  = link_to "Create User", new_admin_user_path, class: 'btn btn-success'
  = link_to "Invite User", admin_invitations_new_path, class: 'btn btn-success'
- if users.any?
  = page_entries_info users
  %table.table
    %thead
      %tr
        %th Email
        %th Full name
        %th Occupation
        %th Country
        %th Roles
        %th Invitation status
        %th Updated at
        %th Created at
        %th Actions
    %tbody
      - users.each do |user|
        %tr
          %td= user.email
          %td= user.decorate.full_name
          %td= user.occupation
          %td= user.country
          %td= user.decorate.display_roles
          %td= user.decorate.invitation_status
          %td= user.updated_at
          %td= user.created_at
          %td
            = link_to admin_user_path(user) do
              %span.glyphicon.glyphicon-search
            = link_to edit_admin_user_path(user) do
              %span.glyphicon.glyphicon-pencil
            - unless user.inactive? or user.admin?
              = link_to admin_user_path(user), method: :delete, data: { confirm: 'Are you sure?' } do
                %span.glyphicon.glyphicon-remove
  = paginate users
- else
  %h3 No users found :(
