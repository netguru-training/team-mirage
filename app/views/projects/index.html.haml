%h2 All Projects

- if user_signed_in?
  =link_to "New Project", new_project_path, class:"btn btn-primary"

%table.table
  %thead
    %tr
      %th= 'Name'
      %th= 'Description'
      %th= 'Goal'
      %th= 'Current Funds'
      %th= 'Status'
  %tbody
    - projects.each do |project|
      %tr
        = render project
